const square = (a) =>{
  return a*a;
};

// const square = (a) => a*a;
// const square = a => a*a;

[1,2,3,4].map(a => a*a);

/*
arrow function does not care who calls it
but regular fumnction care who calls it
*/